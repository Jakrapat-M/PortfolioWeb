import { Center, Container } from "@chakra-ui/react";
import { Grid, Typography } from "@mui/material";
import React from "react";

const Education = () => {
  return (
    <Container id="education" height={"100vh"}>
      <Typography flex={"start"} variant="h4">
        Education
      </Typography>
      <Grid></Grid>
    </Container>
  );
};

export default Education;
